{% extends "layout.njk" %}

{% block content%}

{% block head %}
    <title>Modo de preparado</title>
    <link rel="stylesheet" href="/preparation.css">
{% endblock %}


        <div class="receita">
            <div id="wrapper">
                <img src="{{items.image}}" alt="imagem de {{items.title}}">
            </div>
        </div>

        <div class="titulo">
            <h1>{{items.title}}</h1>
        </div>
        <div class="author">
            <p>Por {{items.author}}</p>
        </div>

        <div class="ingredients">
            <div class="text_ingredients">
                <h1>ingredientes</h1>
                <a class="button">Esconder</a>
            </div>

            <ul class="content show">
                {% for ingredients in items.ingredients %}  
                    <li>{{ingredients}}</li>
                {% endfor %}
            </ul>
        </div>

        <div class="modo">
            <div class="text_modo">
                <h1>Modo de preparo</h1>
                <a class="button">Esconder</a>
            </div>

            <ul class="content show">
                {% for preparation in items.preparation %}  
                    <li>{{preparation}}</li>
                {% endfor %}
            </ul>
        </div>

        <div class="infos">
            <div class="text_info">
                <h1>Informações adicionais</h1>
                <a class="button">Esconder</a>
            </div>

            <ul class="content show">
                <p>{{items.information}}</p>
            </ul>
        </div>

    <footer>
        <p>&copy Todos os direitos reservados, receitas foodfly</p>
    </footer>

    <script src="/scripts.js"></script>
    
{% endblock %}